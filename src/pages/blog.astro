---
import Layout from "../layouts/Layout.astro";
import { Image } from "@astrojs/image/components";
import { sortByDate } from "../utils/sortByDate.js";

import { getCollection } from "astro:content";
const blogEntries = await getCollection("blog", ({ data }) => {
  return data.draft !== true;
});
---

<Layout title="Welcome to Astro.">
  <h1 id="blog">Blog</h1>
  <p>
    Neben meinen Tutorials auf YouTube möchte ich auch einen kleinen Blog starten. Ich hoffe, dass ihr etwas davon habt.
    Ich werde versuchen, regelmäßig neue Artikel zu veröffentlichen. Rechtschreibfehler gehören zu meinem Schreibstil
    und fallen unter Dialekt.
  </p>

  <h2 id="article">Die neusten Artikel</h2>
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-8"></div>
</Layout>
